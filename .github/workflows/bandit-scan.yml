name: Bandit Scan
on:
  push:
    branches: ['securevault-fixed']
  pull_request:
    branches: ['securevault-fixed']
  workflow_dispatch:
jobs:
  bandit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install bandit
      - name: Run Bandit
        run: |
          bandit -r app/ -ll --exit-zero | tee bandit-report.txt
      - name: Generate Security Dashboard Summary
        if: always()
        run: |
          echo "## 🛡️ Bandit Scan Results" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat bandit-report.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
