name: Pylint Scan

on:
  push:
    branches: 
      - devsecops-setup1
      - securevault-full-orchestration
  pull_request:
    branches: 
      - devsecops-setup1
      - securevault-full-orchestration
  workflow_dispatch:

jobs:
  pylint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install dependencies
        run: |
          pip install -r securevault/requirements.txt
          pip install pylint
      - name: Run Pylint
        run: |
          set -o pipefail
          pylint securevault/app/ --fail-under=7 --ignore=migrations | tee pylint-report.txt

      - name: Generate Security Dashboard Summary
        if: always()
        run: |
          echo "## 📝 Pylint Scan Results" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat pylint-report.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
